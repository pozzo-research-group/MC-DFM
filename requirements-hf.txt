# Dependencies for the Hugging Face Space (CPU). See require.txt for the
# pinned local/development environment. Torch is installed from the CPU wheel
# index to keep the Space build small. gradio 6.x is used here (instead of the
# local 5.49.1) because it is compatible with pydantic>=2.12.3, which
# openai-agents requires.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.8.0
numpy==2.3.0
scipy==1.15.1
matplotlib==3.10.8
pandas==2.2.3
scikit-learn==1.6.1
plotly==6.0.0
h5py==3.12.1
openpyxl==3.1.5
nbformat==5.10.4
gsd==3.4.2
openai==2.24.0
openai-agents==0.10.2
agapi==2025.11.15
pydantic>=2.12.3
gradio>=6.0,<7.0
